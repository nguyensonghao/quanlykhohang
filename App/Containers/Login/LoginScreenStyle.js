import { StyleSheet } from 'react-native';

import { Colors } from '../../Themes';

export default StyleSheet.create({
  input: {
    borderColor: '#eeeeee',
    borderWidth: 1
  },
  messageValidate: {
    color: Colors.error
  }
})